eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8888/eureka/
  instance:
    prefer-ip-address: true
server:
  port: 8001
spring:
  application:
    name: gatewayserver
  cloud:
    gateway:
      # routes:
      #   - filters:
      #       - RemoveRequestHeader=Cookie
      #       - StripPrefix=1
      #     id: searchservice
      #     predicates:
      #       - Path=/searchservice/api/v1/**
      #       - Method=GET,POST,PUT,DELETE
      #     uri: lb://searchservice
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
